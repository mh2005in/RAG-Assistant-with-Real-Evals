# Python build artifacts / tooling caches
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local environment / secrets (keep .env.example, ignore the real thing)
.env

# Transient marker the deploy-verify agent writes on a clean pass (consumed by
# the SubagentStop cleanup hook); never committed.
.claude/.deploy-verify-pass
