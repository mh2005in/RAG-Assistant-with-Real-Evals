[project]
name = "rag-assistant"
version = "0.1.0"
description = "RAG assistant built with an evaluation-driven approach"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "mypy>=2.3.0",
    "pytest>=9.1.1",
    "ruff>=0.15.21",
]
